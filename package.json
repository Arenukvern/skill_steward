{
  "name": "skill-steward",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-agent skills marketplace (Cursor, Claude Code, Codex, npx skills)",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "validate": "cd packages/steward_cli && dart pub get && dart run :steward validate",
    "validate:json": "cd packages/steward_cli && dart pub get && dart run :steward validate --json",
    "eval": "node scripts/eval-skill.mjs",
    "eval:json": "node scripts/eval-skill.mjs --json",
    "list": "node scripts/list-skills.mjs",
    "docs:check": "node node_modules/@docs.page/cli/dist/cli.js check",
    "steward:analyze": "cd packages/steward_cli && dart pub get && dart analyze --fatal-infos",
    "steward:validate": "cd packages/steward_cli && dart pub get && dart run :steward validate",
    "steward:list": "cd packages/steward_cli && dart pub get && dart run :steward list",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:status": "changeset status"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0",
    "@docs.page/cli": "^1.0.3",
    "yaml": "2.8.0"
  }
}