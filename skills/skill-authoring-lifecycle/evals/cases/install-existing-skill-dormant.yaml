id: skill-authoring-install-existing-skill-dormant
skill: skill-authoring-lifecycle
routing: should_not_trigger
input: "Install the existing plugin-marketplace-setup skill locally with npx skills add."
rules:
  - kind: file_exists
    path: references/evals.md
  - kind: description_excludes_all
    terms: ["install existing skill locally", "npx skills add only"]
  - kind: body_includes_any
    path: SKILL.md
    terms: ["Add, review, and validate an installable skill package"]
