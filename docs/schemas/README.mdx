# Steward schemas

Language-neutral JSON Schema artifacts for Steward contracts.

These schemas are companion contracts for agents, repo maintainers, and non-Dart harnesses. The Dart validator remains the executable gate, but these files make the data shapes portable.

For the broader Engineering Stewardship baseline that applies before a repo needs typed actions or benchmark scenarios, see [Repo quality contracts](../repo-quality-contracts). This schema directory covers the machine-readable contract layer; it does not replace type-native app/library/tool validation.

| Contract | Schema | Validated by |
|---|---|---|
| `steward.yaml` v1 | [steward-v1.schema.json](steward-v1.schema.json) | `steward doctor`, action/probe/benchmark validators |
| Scenario manifest | [scenario-manifest-v1.schema.json](scenario-manifest-v1.schema.json) | `steward benchmark --scenario <id>` |
| Plugin manifest | [plugin-manifest-v1.schema.json](plugin-manifest-v1.schema.json) | `steward bundle` plugin validation |
| Plugin bundle | [plugin-bundle-v1.schema.json](plugin-bundle-v1.schema.json) | `steward bundle` output validation |
| Plugin bundle index | [plugin-bundle-index-v1.schema.json](plugin-bundle-index-v1.schema.json) | bundle index validation |
| Doctor output | [doctor-v1.schema.json](doctor-v1.schema.json) | `steward doctor --json` |
| Observation record | [observation-v1.schema.json](observation-v1.schema.json) | observation/action-candidate validators |
| Unknown case record | [unknown-case-v1.schema.json](unknown-case-v1.schema.json) | unknown-case promotion validators |
| Action candidate record | [action-candidate-v1.schema.json](action-candidate-v1.schema.json) | action-candidate promotion validators |
| Benchmark summary | [benchmark-summary-v1.schema.json](benchmark-summary-v1.schema.json) | `steward benchmark --json` |
| Adoption run | [adoption-run-v2.schema.json](adoption-run-v2.schema.json) | `steward validate` adoption-run evidence scan |
| Mode event | [mode-event-v1.schema.json](mode-event-v1.schema.json) | `steward protocol validate` |
| Self-model artifact | [self-model-v1.schema.json](self-model-v1.schema.json) | `steward protocol validate` |
| Claim check output | [claim-check-v1.schema.json](claim-check-v1.schema.json) | `steward claim check --json` |
| Blocked explanation output | [blocked-explain-v1.schema.json](blocked-explain-v1.schema.json) | `steward blocked explain --json` |
| Dogfood status output | [dogfood-status-v1.schema.json](dogfood-status-v1.schema.json) | `steward dogfood status --json` |
| Ecology snapshot output | [ecology-snapshot-v1.schema.json](ecology-snapshot-v1.schema.json) | `steward ecology snapshot --json` |
| Ecology route output | [ecology-route-v1.schema.json](ecology-route-v1.schema.json) | `steward ecology route --json` |
| Schema validate output | [schema-validate-v1.schema.json](schema-validate-v1.schema.json) | `steward schema validate --json` |
| Schema check outputs | [schema-check-outputs-v1.schema.json](schema-check-outputs-v1.schema.json) | `steward schema check-outputs --json` |
| Schema emit output | [schema-emit-v1.schema.json](schema-emit-v1.schema.json) | `steward schema emit --json` |
| Schema drift output | [schema-drift-v1.schema.json](schema-drift-v1.schema.json) | `steward schema drift --json` |
| Protocol validate output | [protocol-validate-v1.schema.json](protocol-validate-v1.schema.json) | `steward protocol validate --json` |

Portable schema commands:

```bash
steward schema validate --schema doctor --file .steward/doctor.json --json
steward schema validate --schema mode-event --file .steward/events.jsonl --json
steward schema validate --schema steward/benchmark-summary/v1 --file .steward/benchmark-summaries/run.json --json
steward schema check-outputs --json
steward schema emit --schema schema-validate --source generated --json
steward schema drift --json
```

`schema validate` checks JSON and JSONL artifacts against the repo schema subset used here. Public schema ids and file aliases in the table above are accepted by `schema emit --source checked-in`; `schema validate` is appropriate when the artifact being checked is JSON or JSONL. YAML contracts such as `steward.yaml` and scenario manifests remain native validator targets (`steward doctor`, action/probe/benchmark validators) rather than generic JSON validation targets. `schema check-outputs` validates core read-only CLI payload builders against their schemas so schema/CLI drift is caught before an agent depends on a stale field shape. `schema emit` exposes either checked-in schemas or generated typed-model schemas. `schema drift` compares generated contract model schemas with checked-in schemas for public field, required-field, and mutually declared enum drift. Treat these commands as part of the dogfood path whenever `doctor`, protocol, claim, blocked-state, or benchmark JSON is the evidence route; passing them is not a maturity claim by itself.

Schema policy:

- Required fields match the current v1 hard-cut validator.
- Typed Dart contract models may generate schema metadata for CLI JSON payloads, but checked-in schemas remain the portable review surface for non-Dart consumers.
- Product-specific extension fields are allowed only where the specific schema permits them. Protocol artifacts reject unknown fields by default so private memory or proof claims cannot hide in extension slots.
- Runtime payload schemas describe compact summaries, not raw logs, screenshots, or product artifacts.
- Protocol schemas prove artifact shape only. They do not prove steward status, H4/H5 behavior, consciousness, or final authority.

## Steward vs local harness ownership

Skill Steward owns structural contracts only:

- schema definitions and validator behavior
- `doctor`, action discovery, action inspection, and compact summary shapes
- skill and plugin packaging rules
- promotion criteria and domain-neutral lesson format
- repo-quality contract patterns and evidence expectations

Target repos own execution:

- action implementation and local runner behavior
- raw traces, app artifacts, screenshots, fixtures, and product logs
- product diagnostics, redaction rules, and retention
- repo-local CLI/core/MCP/package layout

Global Steward may delegate only through declared local harness entrypoints and safety gates. It must not interpret product output as domain truth; interpretation belongs to the target repo or to promoted diagnostics backed by reviewed evidence.

## Scenario and benchmark policy

- Scenario manifests live under repo-owned paths such as `steward/scenarios/*.yaml`, not generated `.steward/` records.
- `source.steward_contract` and file-backed scenario manifests must be clean and tracked before actions run.
- `durability_blocked` is a valid blocked benchmark result when contract inputs are modified or untracked; it is not H2 proof.
- Strict proof blocks broad-read actions when undeclared dirty files could affect what the action observes.
- `source.commit` is the subject commit under test. It must not be treated as proof that a scenario manifest contains itself at that same commit.
- Benchmark summaries prove the manifest input with `scenario_manifest_sha256`, `input_digests`, durability, proof, and result. They do not prove remote reproducibility unless the runner verifies fetchability from `source.git`.
- Benchmark summaries must not include raw stdout/stderr, raw product traces, screenshots, secrets, or large artifacts.

## Validator responsibilities

JSON Schema covers portable shape checks. Executable validators remain responsible for cross-field and repository-state checks, including:

- action references exist and are quick-eligible before quick probes run
- scenario manifests use durable git URLs, resolved commit SHAs, steward contract paths, owners, and required actions
- local paths are not used as durable provenance
- action effects declare explicit `fs_read` and `fs_write`
- auto/quick actions do not require confirmation, shell, network, secrets, destructive behavior, or repo mutation
- plugin artifacts use managed blocks and uninstall metadata
- diagnostics and action candidates follow the promotion state machine
- benchmark runs do not promote diagnostics or actions created in the same run
- adoption-run detours enforce stop semantics after two repair/setup attempts
- H5/S5 adoption-run claims require repeated evidence plus `held_out_benchmarks`
- repo-maturity adoption-run claims require explicit broad evidence
- adoption-run proof needs `hot_path_claim.observed_effect`; a success return code alone is not promotion evidence
- stewardship protocol validation checks mode-event and self-model artifact shape plus raw private-material tripwires; it does not promote `proven_repo_steward`
- stewardship claim checks are negative gates against obvious overclaims; they do not accept claims or reason over evidence quality
- blocked-state explanations route `durability_blocked`, `blocked_invalid_config`, and runtime proof blocks into next actions; they do not repair the repo or turn blocked evidence into proof
- schema drift and output checks keep the machine-readable dogfood path aligned; they do not accept adoption, steward-status, or repo-maturity claims
