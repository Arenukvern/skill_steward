---
"skill-steward": patch
---

Generalize the `mcp-harness-repo-maintainer` skill and all other skills, documentation, and Architectural Decision Records (ADRs) to remove specific framework and repository references (e.g., `mcp_flutter`, `IntentCall`, `flutter_harness`), replacing them with abstract framework-agnostic descriptors.
