---
"skill-steward": patch
---

Gate direct GitHub Release asset jobs on the resolved release tag instead of the Changesets `published` output, which stays false for the repo's custom tag-only publish script.
