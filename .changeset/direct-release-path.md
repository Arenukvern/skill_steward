---
"skill-steward": patch
---

Create GitHub Releases directly in the release workflow after Changesets publishes, including changelog notes and binary artifacts, instead of relying on a tag-triggered workflow suppressed by `GITHUB_TOKEN`.
